worker-threads: 4
worker-fs-threads: 2
worker-grpc-threads: 2
logger-level: debug

is-testing: true

rofls-endpoint: '[::1]:8081'

# simple_db_calls_db is the service name + _ + filename of the db.sql
dbconnection: 'postgresql://testsuite@localhost:15433/simple_db_calls_db'
